node_modules/
dist/

# Saved Playwright session — contains live auth cookies. Never commit.
auth.json

# Local secrets/config for the auto-start installer (token, ngrok domain).
.env.local

*.log
.DS_Store
logs/
